version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - DB_HOST=192.168.141.129
      - DB_USER=devops_user
      - DB_PASSWORD=SecurePass123!
      - DB_NAME=upwork_db
    restart: always
